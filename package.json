{
  "name": "tutorial",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy:wrangler": "npm run build && npx wrangler publish --site ./dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
     "astro": "^7.0.6",
    "preact": "^10.11.0"
  }
}
